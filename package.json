{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "turbo dev",
    "lint": "turbo lint --",
    "format": "prettier --write '{apps,packages}/**/*.{js,jsx,ts,tsx,json,md}'",
    "build": "turbo build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "apps/*",
    "shared-configs/*"
  ],
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "husky": "^8.0.3",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.2.4",
    "turbo": "^1.11.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint && npm run format"
    }
  }
}